import { useParams } from 'react-router-dom'; import MarketplaceLayout from '../components/MarketplaceLayout'; import ProductCard from '../components/ProductCard'; import { products,vendors } from '../data';
export default function VendorDetail(){const {id}=useParams();const vendor=vendors.find(v=>v.id===Number(id))||vendors[0];return <MarketplaceLayout><main className="page-container"><section className="store-hero"><div className="store-avatar">{vendor.name.slice(0,2).toUpperCase()}</div><div><small>VERIFIED VENDOR</small><h1>{vendor.name}</h1><p>{vendor.category} • {vendor.products} products • ★ {vendor.rating}</p></div><button className="outline-btn">Follow store</button></section><div className="section-heading"><div><small>STORE PRODUCTS</small><h2>Products from {vendor.name}</h2></div></div><div className="product-grid">{products.filter(p=>p.vendor===vendor.name).concat(products.slice(0,4)).slice(0,4).map((p,i)=><ProductCard key={`${p.id}-${i}`} product={{...p,vendor:vendor.name}}/>)}</div></main></MarketplaceLayout>}
