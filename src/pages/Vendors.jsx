import { Link } from 'react-router-dom'; import MarketplaceLayout from '../components/MarketplaceLayout'; import { vendors } from '../data';
export default function Vendors(){return <MarketplaceLayout><main className="page-container"><div className="page-title"><small>MARKETPLACE STORES</small><h1>Featured vendors</h1><p>Discover trusted sellers and shop directly from their stores.</p></div><div className="vendor-grid large">{vendors.map(v=><Link className="vendor-card" to={`/vendors/${v.id}`} key={v.id}><div className="vendor-avatar big">{v.name.slice(0,2).toUpperCase()}</div><div><strong>{v.name}</strong><p>{v.category} • {v.products} products</p><span>★ {v.rating} • Verified vendor</span></div><b>→</b></Link>)}</div></main></MarketplaceLayout>}
