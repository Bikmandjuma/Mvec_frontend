import { Link } from 'react-router-dom'; import MarketplaceLayout from '../components/MarketplaceLayout'; import { useLocalStorage } from '../hooks';
export default function Orders(){const [orders]=useLocalStorage('mvec_orders',[]);return <MarketplaceLayout><main className="page-container"><div className="page-title"><small>BUYER CENTER</small><h1>My orders</h1></div>{!orders.length?<div className="empty-state"><div>▣</div><h2>No orders yet</h2><p>Your purchases will appear here.</p><Link to="/shop" className="gradient-btn">Start shopping</Link></div>:<div className="orders-list">{orders.map(o=><article className="order-card" key={o.id}><div className="order-head"><strong>{o.id}</strong><span>{o.date}</span><b className={`order-status ${o.status.toLowerCase()}`}>{o.status}</b><b>Payment: {o.paymentStatus}</b></div><div className="order-items">{o.items?.map(p=><div key={p.id}><img src={p.image} alt=""/><span>{p.name} × {p.quantity}</span><b>{(p.price*p.quantity).toLocaleString()} RWF</b></div>)}</div><div className="order-foot"><span>Total <strong>{o.total.toLocaleString()} RWF</strong></span><Link to={`/payment/${o.id}`}>View payment</Link></div></article>)}</div>}</main></MarketplaceLayout>}
